rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read and write for authenticated users, and public read for shared previews
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
